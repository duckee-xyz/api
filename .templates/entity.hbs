import { Column, CreateDateColumn, Entity, PrimaryGeneratedColumn, UpdateDateColumn } from 'typeorm';
import { {{ pascalCase name }} } from '../models';

@Entity({ name: '{{ snakeCase name}}' })
export class {{ pascalCase name }}Entity {
  @PrimaryGeneratedColumn()
  id: number;

  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;

  toModel(): {{ pascalCase name }}  {
    return {
      id: this.id,
    };
  }
}
