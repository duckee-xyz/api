import { Route, Tags } from '@tsoa/runtime';
import { Service } from 'typedi';
import { {{ pascalCase name }}Repository } from './{{ pascalCase name }}Repository';

@Service()
@Tags('{{ titleCase name }}')
@Route('/{{ dashCase name }}/v1')
export class {{ pascalCase name }}Controller {
  constructor(private {{ snakeCase name }}Repository: {{ pascalCase name }}Repository) {}
}
