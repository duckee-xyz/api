import { Service } from 'typedi';
import { Repository } from 'typeorm';
import { InjectRepository } from '~/utils';
import { {{ pascalCase name }}Entity } from './entities';

@Service()
export class {{ pascalCase name }}Repository {
  constructor(@InjectRepository({{ pascalCase name }}Entity) private {{ snakeCase name }}Repo: Repository<{{ pascalCase name }}Entity>) {}
}
